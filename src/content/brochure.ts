/**
 * 서비스 소개서 파일 — 경로·크기·목차 정본.
 *
 * ── 왜 한 곳에 모으나 ──
 * 신청 화면(`/brochure`)은 "무엇이 들어 있는지" 를 말하고, 완료 화면
 * (`/brochure/complete`)은 그 파일을 내려준다. 경로와 크기를 두 화면에 각각
 * 적으면 파일을 교체할 때 한쪽만 고쳐진다.
 *
 * ── 파일 이름을 바꿔서 넣었다 ──
 * 원본은 `[반오토] 서비스 소개서_0828.pdf` 다. 대괄호·공백·한글이 든 이름은
 * URL 에서 퍼센트 인코딩되어 `%5B%EB%B0%98...` 처럼 읽을 수 없는 주소가 되고,
 * 일부 메신저·메일 클라이언트가 링크를 중간에서 끊는다. 그래서 배포용 이름은
 * 영문·하이픈으로 바꾸고 **날짜를 남겼다** — 다음 판이 오면 파일명이 달라져
 * 캐시가 자동으로 갈린다(같은 이름으로 덮으면 옛 파일이 계속 내려간다).
 *
 * ⚠️ 파일을 교체할 때 `size`·`pages`·`updated` 를 **함께** 고친다. 화면이 이 값을
 *    그대로 보여주므로 어긋나면 곧 거짓말이 된다(14.5MB 라고 적어 두고 30MB 를
 *    내려보내는 상황).
 */

/** 배포되는 소개서 파일 */
export const BROCHURE_FILE = {
  /** `public/` 기준 경로 — 그대로 `href` 에 쓴다 */
  href: "/brochure/bahnauto-service-guide-2026-08.pdf",
  /** 내려받을 때 붙는 이름. 한글을 써도 `download` 속성이 처리한다 */
  downloadName: "반오토 서비스 소개서.pdf",
  /** 실측값(2026-08-28). 다운로드 전에 알려 준다 — 모바일 데이터로 받는 사람이 있다 */
  size: "약 15MB",
  pages: 25,
  updated: "2026-08-28",
} as const;

/**
 * 소개서 목차 — **PDF 2쪽 `CONTENTS` 를 그대로 옮겼다.**
 *
 * 이전에는 파일이 없어 "관리 범위와 기능 / 요금 구성 / 도입 절차" 로 **추측해서**
 * 적어 두었다. 실제 문서를 열어 보니 6개 장이었다. 받기 전에 무엇이 들어 있는지
 * 알려 주는 자리라 **문서에 실제로 있는 것만** 적는다(CLAUDE.md §5).
 */
export const BROCHURE_CONTENTS: readonly { no: string; title: string; body: string }[] = [
  { no: "01", title: "WHY 반오토", body: "무인매장 운영, 관리는 그대로인가요?" },
  { no: "02", title: "서비스 소개", body: "반오토란 무엇인가 · 대시보드 + 인력 구조" },
  { no: "03", title: "서비스 상세", body: "운영 대시보드 · 운영 인력 지원" },
  { no: "04", title: "이용 안내", body: "도입 절차 · 요금제 · 적용 업종" },
  { no: "05", title: "신뢰 요소", body: "왜 반오토인가" },
  { no: "06", title: "FAQ", body: "자주 묻는 질문 · 문의" },
];
