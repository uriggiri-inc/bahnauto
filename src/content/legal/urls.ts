/**
 * 법정 문서의 **공개 원본 주소** — 2026-09-02 부터 이곳이 정본이다.
 *
 * 그 전까지 푸터는 사이트 안의 `/terms` · `/privacy` 로 갔고, 그 페이지들은
 * `content/legal/{terms,privacy}.ts` 에 옮겨 적은 사본을 보여줬다. 법무가 문구를
 * 고칠 때마다 개발자가 옮겨 적어야 했고 그 사이 두 문서가 어긋났다.
 * **노션 원본으로 직접 보내면 고친 즉시 반영된다**(사용자 지시).
 *
 * ⚠️ 두 주소 모두 **로그인 없이 열려야 한다.** 개인정보보호법 제30조 ② 는 방침의
 *    공개를 요구한다. 2026-09-02 확인: 노션 `publicAccessRole: reader` ·
 *    `requireLogin: false` — 공개 상태다.
 *    **노션에서 공유를 내리면 그 순간 사이트가 법정 고지를 잃는다.** 워크스페이스
 *    정리 중에 조용히 끊기기 쉬운 지점이라, 주소를 바꾸거나 공유를 손댈 때는
 *    반드시 시크릿 창으로 다시 확인한다.
 *
 * ⚠️ 사이트 안의 `/terms` · `/privacy` 라우트는 아직 남아 있다. 이제 푸터가
 *    가리키지 않으므로 **링크로는 닿지 않지만 주소를 알면 열린다.** 그 안의 사본은
 *    갱신되지 않으므로 노션 원본과 갈라진다 — 처리 방침을 정할 때까지의 임시
 *    상태다(사이트맵에서 뺄지, 이정표로 바꿀지, 남길지).
 */

export const LEGAL_URLS = {
  terms: "https://uriggiri.notion.site/_-260901-3cfb31a76f638004bb13d17d08af0467",
  privacy: "https://uriggiri.notion.site/_-260901-3cfb31a76f6380b38c0ae55a26c733db",
} as const;
