/**
 * 반오토 주요기능 **7종** — 확정본이다.
 *
 * ── 2026-08-27 8종 → 7종 전면 재편 (담당자 전달: `반오토_요금제_카드텍스트(수정).docx`) ──
 * 카피는 그 문서의 표를 **그대로** 옮겼다. 화면 목업(`홈페이지_수정.pptx` 2번 장)과
 * 문구가 한 곳 다른데(⑤ 요약), **문서 파일명이 "(수정)" 이라 문서를 정본으로 잡았다.**
 *
 * 무엇이 어디로 갔는지 — 이 표가 없으면 나중에 추적이 불가능하다:
 *
 * | 새 카드 | 이전 8종에서 |
 * |---|---|
 * | ① 운영 대시보드 | `dashboard` + **`manager-support` · `docs` 흡수** |
 * | ② 기본서비스 | 신규 (예약 셋팅·행정 대행) |
 * | ③ 경영지원 | 옛 `revenue` + `place` — **이름만 옛 경영지원과 같고 내용이 다르다** |
 * | ④ 고객센터 | 옛 `b2c` (매장 고객 응대센터) |
 * | ⑤ 현장 운영 지원 | **옛 `ops-support`(경영지원)의 내용이 그대로 여기로** |
 * | ⑥ 방문관리서비스 | 옛 `dispatch` |
 * | ⑦ A/S 바로출동서비스 | 신규 · 준비 중 |
 *
 * ⚠️ **키를 제목 뜻에 맞춰 새로 붙였다.** 옛 키를 재활용하면 `ops-support`(경영지원)
 *    키가 "현장 운영 지원" 을 가리키고 `revenue` 가 "경영지원" 을 가리켜, 코드를 읽는
 *    사람이 매번 이 표를 찾아봐야 한다. 주소가 바뀌는 대가를 지금 치르는 이유는
 *    **검색 노출이 아직 막혀 있어서**(`lib/seo.ts` 의 `SEARCH_OPEN = false`) 지금이
 *    가장 싼 시점이기 때문이다. 정적 배포라 리다이렉트를 걸 수 없으므로, 오픈 뒤에
 *    바꾸면 이미 나간 링크가 그냥 죽는다.
 *
 * 명칭은 섹션마다 다르게 부르지 않는다 — **화면에는 이 파일의 이름만 쓴다.**
 *
 * ⚠️ 2026-08-28: ⑥·⑦ 이름에 **띄어쓰기가 들어갔다**(`방문관리서비스` →
 *    `방문관리 서비스`, `A/S 바로출동서비스` → `A/S 바로출동 서비스`).
 *    담당자 `반오토 주요기능 수정.html` 의 표기를 따랐다 — 그 문서가 주요기능
 *    카피의 정본이다(사용자 지시). 이름은 홈 카드·사이드바·요금 옵션 목록에
 *    함께 나오므로 세 곳이 한 번에 바뀐다.
 *
 * 아래 `bullets` 는 **`.docx` 정본**이고 상세 페이지 카피는 **HTML 정본**이다.
 * 두 문서가 서로 절반씩만 적어 화면이 갈려 보이던 두 곳을 2026-09-08 에 맞췄다:
 *
 * | 어디 | 무엇이 갈렸나 | 어떻게 맞췄나 |
 * |---|---|---|
 * | ④ 고객센터 | 여기는 "24시간 고객상담 챗봇 제공", HTML 은 "오전 9시 ~ 오후 9시 운영" | **둘 다 맞다** — 챗봇 24시간, 본사 연결 9 to 9(`plans.ts` 확정). 여기는 그대로 두고 `feature-details.ts` 칩에 두 사실을 함께 넣었다 |
 * | ⑥ 방문관리 | `summary` 는 "정기적으로", `bullets` 는 "비정기점검" — **한 카드 안에서** 어긋났다 | 상세 페이지 카드가 정기 점검과 요청 기반 대응을 **둘 다** 적고 있다. `bullets` 를 그 두 가지가 보이게 고쳤다(아래) |
 *
 * ⚠️ 방문관리 점검 **주기**(분기·월 등)를 명시한 정본은 어디에도 없다. 후기에
 *    주기를 말한 문장이 있어도 그것은 점주 진술이지 서비스 사양이 아니다 —
 *    사이트 카피에 옮기지 않는다.
 *
 * ⚠️ 여기 값은 더미가 아니다. `dummy.ts` 게이트와 무관하게 항상 노출된다.
 *    금액은 이 파일에 두지 않는다 — 요금은 `plans.ts` 가 정본이다.
 */

import type { ServiceIconName } from "@/components/marketing/serviceIcons";

export type Feature = {
  /** 요금제 구성·상세 페이지 주소·캐러셀이 이 키로 기능을 가리킨다 */
  key: string;
  /** 카드 제목 */
  title: string;
  /** 카드 한 줄 요약 */
  summary: string;
  /** 카드의 포함 항목 */
  bullets: readonly string[];
  icon: ServiceIconName;
  /**
   * 아직 열지 않은 기능. 카드가 회색으로 내려앉고 요금제 옵션에도 "준비 중" 으로
   * 표시된다(수정안 목업 그대로).
   *
   * 상세 페이지의 오픈 예정 띠는 이 값이 아니라 `feature-details.ts` 의 `notice` 가
   * 그린다 — 그쪽은 "언제 여는지" 같은 문장을 함께 담아야 하기 때문이다.
   */
  comingSoon?: boolean;
};

export const FEATURES: readonly Feature[] = [
  {
    key: "dashboard",
    title: "운영 대시보드(PC/모바일)",
    summary: "매장에서 일어난 일이 한 화면에 모입니다",
    bullets: [
      "업무 체크리스트·재고 관리·출퇴근 관리",
      "매니저 지원센터(매뉴얼·CS 창구)",
      "인허가·서류 관리",
    ],
    icon: "checklist",
  },
  {
    key: "basic",
    title: "기본서비스",
    summary: "매장 운영 기본 행정 및 셋팅을 지원해 드립니다",
    bullets: ["네이버 예약 셋팅·예약 관리·알림톡 발송", "행정업무 대행"],
    icon: "admin",
  },
  {
    key: "biz-support",
    title: "경영지원",
    summary: "마케팅부터 매출까지 함께 챙깁니다",
    bullets: [
      "스마트플레이스 관리·지도점검 컨설팅",
      "온라인 SNS마케팅 지원(쿠폰 발행, 콘텐츠 업로드)",
      "검색광고 예산 검토",
      "월간 매출관리 리포트 제공",
    ],
    icon: "stock",
  },
  {
    key: "customer-center",
    title: "고객센터",
    summary: "매장 이용고객의 문의를 대신 받습니다",
    bullets: ["24시간 고객상담 챗봇 제공", "9 to 9 본사 고객센터 연결"],
    icon: "support",
  },
  {
    key: "field-ops",
    title: "현장 운영 지원",
    summary: "매니저 채용부터 매장 운영 전반을 담당합니다",
    bullets: [
      "매니저 채용·근태관리",
      "발주 대행·매점 상품 관리",
      "회계(인건비·발주 실비 정산 청구)",
      "외부업체 및 관리사무소 등 소통 대행",
    ],
    icon: "manager",
  },
  {
    /*
      `bullets` 는 2026-09-08 에 손봤다. `.docx` 는 "시설팀 비정기점검 / 유지보수
      방문관리 / 에어컨 분해청소" 였는데 바로 위 `summary` 의 "정기적으로" 와
      한 카드 안에서 어긋났다. 상세 페이지(`feature-details.ts`)를 보면 둘 다
      실재한다 — `정기 시설관리` 카드와 `이슈 접수·업체 연결` 카드다. 그래서
      정기 쪽을 살리고 요청 기반 쪽을 상세 카드 제목 그대로 한 줄 추가했다.
      문구는 전부 다른 파일에서 가져온 것이다:
        · "시설팀 방문관리"     ← plans.ts  PLAN_COMPOSITION visit-care
        · "정기 점검·유지보수"  ← feature-details.ts ⑥ 칩
        · "이슈 접수·업체 연결" ← feature-details.ts ⑥ 핵심 기능 카드 제목
      ⚠️ 주기(분기·월 등)는 적지 않는다 — 머리 주석 참고.
    */
    key: "visit-care",
    title: "방문관리 서비스",
    summary: "전문 인력·업체가 직접 찾아가 정기적으로 점검합니다",
    bullets: ["시설팀 방문관리", "정기 점검·유지보수", "이슈 접수·업체 연결", "에어컨 분해청소"],
    icon: "dispatch",
  },
  {
    key: "as-dispatch",
    title: "A/S 바로출동 서비스",
    summary: "시설 고장 및 긴급상황 출동 서비스",
    bullets: ["준비 중"],
    icon: "emergency",
    comingSoon: true,
  },
];

/** 기능 키로 빠르게 찾는다 — 요금제 카드가 기능 목록을 그릴 때 쓴다 */
export const FEATURE_BY_KEY = Object.fromEntries(FEATURES.map((f) => [f.key, f])) as Record<
  string,
  Feature
>;
