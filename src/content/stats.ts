/**
 * 실적·자격 수치 — **검증된 확정값**.
 *
 * 2026-09-02 사용자가 근거와 함께 확정했다. 그 전까지 이 자리에는 `dummy.ts` 의
 * `DUMMY_STATS`(48개 가맹점 · 94% 재계약률 · 12,400회 누적 방문 관리 ·
 * 36,000명 연간 이용자)가 있었고, 근거가 없어 게시 금지 항목이었다.
 * **그 네 값은 폐기됐다** — 되살리지 않는다.
 *
 * ⚠️ 표시광고법상 이 수치는 **실증할 수 있어야 한다.** 값을 고칠 때는 아래
 *    근거를 함께 갱신한다. 근거 없는 수치로 바꾸지 않는다.
 *
 * ── 근거 (2026-09-02 사용자 제공) ──
 * · 국내 최초      2021년 무인키즈카페 프랜차이즈 가맹사업 등록
 * · 가맹점 수 127개  2026년 9월 기준
 * · 디자인 등록 9종  2023년 디자인특허 등록 (조합놀이대 · 라벨)
 * · 특허 보유       2023년 운영서비스 제공 시스템 BM특허 등록
 *
 * ⚠️ **`가맹점 수` 는 변동값이다.** 주기적으로 재확인해 갱신한다 — 오래된 수치를
 *    현재값처럼 두면 그 자체가 부당표시가 된다.
 *
 * ── "국내 최초" 에 대하여 ──
 * `CLAUDE.md` §5 의 금지어(`업계 최초` · `국내 유일`)와 같은 유형의 최상급
 * 표현이다. **사용자가 근거를 확인하고 그대로 쓰기로 결정했다(2026-09-02).**
 * 이 예외는 **이 항목 하나에만** 적용된다 — 다른 카피에 최상급 표현을 새로
 * 넣지 않는다.
 *
 * 순서는 사용자 지정이다(자격 → 규모 → 자격). 바꾸지 않는다.
 */

export type SiteStat = {
  /** 숫자면 3자리 구분 포맷 문자열. 숫자가 아니면 그대로 정적 렌더된다 */
  value: string;
  unit?: string;
  label: string;
};

export const SITE_STATS: readonly SiteStat[] = [
  { value: "국내 최초", label: "무인 키즈카페 프랜차이즈" },
  { value: "127", unit: "개", label: "가맹점 수" },
  { value: "9", unit: "종", label: "컨셉별 디자인 등록" },
  { value: "특허 보유", label: "운영서비스 제공 시스템" },
];

/**
 * 기본 요금 — 운영 대시보드(PC/모바일) 월 요금. **원/월, VAT 별도.**
 *
 * 2026-09-02 사용자가 **내부 확정 금액**임을 확인했다. 그 전까지는 `dummy.ts` 의
 * `DUMMY_BASE_PRICE` 였고 "담당자 지정값 · 근거 미확보" 로 표시돼 있었다.
 *
 * 사이트에 남은 **유일한 공개 금액**이다. 옵션 6종은 전부 "도입 상담에서 안내" 다.
 *
 * ⚠️ 금액을 바꾸면 `content/faq.ts` 의 요금 문항과 `content/plans.ts` 의 안내
 *    문장(VAT 별도 · 옵션 조합은 상담)이 함께 맞는지 확인한다.
 */
export const BASE_PRICE = 24_900;
